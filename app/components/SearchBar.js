import styled from "styled-components";

export default (SearchBar = styled.View`
  background: #e7e7e7;
  margin: 0 15px;
  height: 40px;
  padding-left: 10px;
  padding-right: 10px;
  border-radius: 10px;
  flex-direction: row;
  align-items: center;
`);
